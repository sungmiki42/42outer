{
  "image": "ubuntu:latest",
  "runArgs": ["--platform", "linux/amd64"],
  "postCreateCommand": "apt-get update && apt-get install -y sudo git nasm build-essential && git config --global user.name sungmiki42 && git config --global user.email sungmiki@student.42seoul.kr",
  "remoteUser": "root"
}